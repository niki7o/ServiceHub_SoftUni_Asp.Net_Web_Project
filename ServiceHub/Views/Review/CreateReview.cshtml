@using ServiceHub.Core.Models.Reviews
@model ReviewFormModel

@{
    ViewData["Title"] = "Добави ревю";
    Guid serviceId = ViewBag.ServiceId;
    string serviceTitle = ViewBag.ServiceTitle;
    Layout = "_Layout"; 
}
<link rel="stylesheet" href="~/css/createReview.css" asp-append-version="true" />


<div class="container">
    <h1>Добави ревю за @serviceTitle</h1>

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger" role="alert">
            @TempData["ErrorMessage"]
        </div>
    }

    <div class="row">
        <div class="col-md-8 offset-md-2">
            <form asp-controller="Review" asp-action="AddReview" asp-route-serviceId="@serviceId" method="post">
                @Html.AntiForgeryToken()

                <div class="form-group mb-3">
                    <label asp-for="Rating" class="form-label">Оценка (1-5):</label>
                    <div class="rating-stars-form">
                        @for (int i = 5; i >= 1; i--)
                        {
                            
                            <input asp-for="Rating" type="radio" id="ratingStar@(i)" value="@i" required />
                            <label for="ratingStar@(i)"><i class="fas fa-star"></i></label>
                        }
                    </div>
                    <span asp-validation-for="Rating" class="text-danger"></span>
                </div>

                <div class="form-group mb-4">
                    <label asp-for="Comment" class="form-label">Коментар:</label>
                    <textarea asp-for="Comment" class="form-control" rows="5" placeholder="Напишете вашия коментар тук..."></textarea>
                    <span asp-validation-for="Comment" class="text-danger"></span>
                </div>

                <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                    <button type="submit" class="btn btn-primary">Изпрати ревю</button>
                   
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
