@{
    ViewData["Title"] = "Страницата не е намерена";
    Layout = "_Layout"; 
}

<style>
    
    .error-container-wrapper {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: calc(100vh - var(--header-height, 80px) - var(--footer-height, 60px)); 
        text-align: center;
        padding: 40px 20px; 
        max-width: 900px;
        margin: 50px auto;
        background-color: #264653;
        color: #f8edeb; 
        border-radius: 15px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.4);
        animation: fadeIn 1s ease-out forwards;
    }

    .error-code {
        font-size: 12rem;
        font-weight: 900;
        color: #e76f51; 
        text-shadow: 5px 5px 15px rgba(0,0,0,0.5);
        margin-bottom: 0px;
        line-height: 1;
        animation: bounceIn 1.2s ease-out forwards;
        opacity: 0; 
    }

    .error-header {
        font-size: 3.2rem; 
        font-weight: 700;
        color: #f4a261; 
        margin-top: 10px;
        margin-bottom: 20px;
        text-shadow: 2px 2px 5px rgba(0,0,0,0.3);
    }

    .error-subheader {
        font-size: 1.6rem; 
        color: #e9c46a; 
        margin-bottom: 40px;
        line-height: 1.6;
        opacity: 0.9;
    }

    .button-container {
        margin-top: 40px;
        display: flex;
        gap: 20px;
        flex-wrap: wrap;
        justify-content: center;
    }

    .action-button {
        background-color: #2a9d8f; 
        color: white;
        padding: 18px 35px;
        border-radius: 10px;
        text-decoration: none;
        font-size: 1.2rem;
        font-weight: 600;
        transition: all 0.3s ease-in-out;
        box-shadow: 0 6px 15px rgba(0,0,0,0.3);
        border: none;
        cursor: pointer;
    }

        .action-button:hover {
            background-color: #21887a;
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.4);
        }

    /* Анимации */
    @@keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(20px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @@keyframes bounceIn {
        0% {
            opacity: 0;
            transform: scale(0.3);
        }

        50% {
            opacity: 1;
            transform: scale(1.1);
        }

        70% {
            transform: scale(0.9);
        }

        100% {
            opacity: 1;
            transform: scale(1);
        }
    }

    /* Адаптивен дизайн */
    @@media (max-width: 768px) {
        .error-code {
            font-size: 8rem;
        }

        .error-header {
            font-size: 2.5rem;
        }

        .error-subheader {
            font-size: 1.3rem;
        }

        .action-button {
            padding: 15px 30px;
            font-size: 1.1rem;
        }

        .button-container {
            flex-direction: column;
            gap: 15px;
        }
    }

    @@media (max-width: 480px) {
        .error-code {
            font-size: 6rem;
        }

        .error-header {
            font-size: 2rem;
        }

        .error-subheader {
            font-size: 1rem;
        }

        .action-button {
            width: 100%;
        }
    }
</style>

<div class="error-container-wrapper">
    <h1 class="error-code">404</h1>
    <h2 class="error-header">Страницата не е намерена</h2>
    <p class="error-subheader">
        Извиняваме се, но страницата, която търсите, не съществува или е била преместена.
        Моля, проверете URL адреса или използвайте бутоните по-долу.
    </p>
    <div class="button-container">
        <button onclick="history.back()" class="action-button">Върнете се назад</button>
        <a asp-area="" asp-controller="Home" asp-action="Index" class="action-button">Към Начална страница</a>
    </div>
</div>
